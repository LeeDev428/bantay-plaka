# ANPR Engine dependencies
# Install with: pip install -r anpr_engine/anpr_requirements.txt
#
# Install in the SAME venv as your Django project.

# Camera frame reading
opencv-python>=4.9.0

# Plate text recognition (OCR)
easyocr>=1.7.1

# Roboflow inference SDK (PRIMARY detector -- 98.8% accuracy)
# Downloads and runs the Roboflow-trained plate model locally after first use.
inference>=0.9.0

# GPU-accelerated ONNX Runtime -- required for Roboflow inference to use CUDA.
# Replaces onnxruntime (CPU-only). If onnxruntime is already installed, run:
#   pip uninstall onnxruntime -y && pip install onnxruntime-gpu
onnxruntime-gpu>=1.17.0

# YOLO (fallback detector -- only needed if using --mode yolo)
ultralytics>=8.2.0

# HTTP requests to Django
requests>=2.31.0

# Read .env file
python-dotenv>=1.0.0

numpy>=1.26.0
